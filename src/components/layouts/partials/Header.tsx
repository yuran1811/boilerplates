import { FC } from 'react';

export const Header: FC = () => (
  <header className="z-[100] sticky top-0 left-0 w-full border-b-[2px] p-4 flexcenter flex-wrap">
    <div className="text-center">Header</div>
  </header>
);
